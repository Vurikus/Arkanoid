public class Z {

    //Field

    // Constructor

    //Function
/*
    protected static void fillFieldBlocks(){
        for (int i = 0; i < GamePanel.WIDTH_FRAME / Block.widthBlock; i++) {
            for (int j = 0; j <= 16; j++) {
                GamePanel.blocks.add(block = new Block(i * Block.widthBlock, j * Block.heightBlock, Setting.getColorBlock(0)));
            }
        }
    }

    private void createBlocksMap_One(){
        for (int i = 0; i < field1.size(); i++) {
            int x = 0;
            int y = 0;
            if (i==15) {
                x = 0;
                y++;
            }
            if(field1.get(i).equals(t)) {
                GamePanel.blocks.add(new Block(x * Block.widthBlock, y * Block.heightBlock, Color.BLUE));
            }
            x++;
        }
    }




*/

}
